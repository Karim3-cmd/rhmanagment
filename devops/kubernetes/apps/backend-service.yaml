apiVersion: v1
kind: Service
metadata:
  name: hrbrain-backend
  namespace: hrbrain
  labels:
    app: hrbrain-backend
spec:
  type: ClusterIP
  selector:
    app: hrbrain-backend
  ports:
    - name: http
      port: 3000
      targetPort: 3000
