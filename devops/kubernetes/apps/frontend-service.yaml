apiVersion: v1
kind: Service
metadata:
  name: hrbrain-frontend
  namespace: hrbrain
  labels:
    app: hrbrain-frontend
spec:
  type: ClusterIP
  selector:
    app: hrbrain-frontend
  ports:
    - name: http
      port: 80
      targetPort: 80
